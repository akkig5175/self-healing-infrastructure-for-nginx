groups:
- name: nginx_alerts
  rules:
  - alert: NginxDown
    expr: probe_success{job="nginx-probe"} == 0
    for: 10s
    labels:
      severity: critical
    annotations:
      description: "Nginx is not responding on port 8080"
      summary: "Nginx service is DOWN"
  